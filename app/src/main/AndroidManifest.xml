<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tehhutan.app.kajianhunter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDENIQhcWz79PaZu7eTQeeSHmfmxRtBHtg" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".Login_Register.Login" /> -->
        <activity android:name=".Login_Register.Login"></activity>
        <activity
            android:name=".Login_Register.Register"
            android:label="Daftar Akun" />
        <activity
            android:name=".Edit_Profile"
            android:label="Ubah Profile" />
        <activity
            android:name=".Login_Register_Act"
            android:label="Kajian Hunter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PostDanKomentar"></activity>
    </application>

</manifest>